/**
 * SMD Box design system: tokens + presentational components.
 *
 * This is the discoverable surface for Claude Design `/design-sync` and for
 * reuse. Components here are pure/presentational (no store or data access).
 */
export { tokens, type Tokens } from './tokens';
export { CodeChip } from './components/CodeChip';
export { SectionTitle } from './components/SectionTitle';
export { RequiredMark } from './components/RequiredMark';
